<UserControl x:Class="CodexNativeAgent.Ui.StatusBarPaneControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
        <UserControl.Resources>
            <Style x:Key="StatusBarHoverToolTipStyle" TargetType="ToolTip">
                <Setter Property="Background" Value="{DynamicResource TranscriptBubbleGlassStrongBrush}" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="10,7" />
            </Style>
            <Style x:Key="NoOutlineBubbleComboBoxItemStyle"
                   TargetType="ComboBoxItem"
                   BasedOn="{StaticResource {x:Type ComboBoxItem}}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="ItemChrome"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    BorderThickness="0"
                                    CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="ItemChrome" Property="Background" Value="{DynamicResource TranscriptBubbleGlassStrongBrush}" />
                                    <Setter TargetName="ItemChrome" Property="BorderBrush" Value="Transparent" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="ItemChrome" Property="Background" Value="{DynamicResource TranscriptBubbleGlassStrongBrush}" />
                                    <Setter TargetName="ItemChrome" Property="BorderBrush" Value="Transparent" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="ItemChrome" Property="Opacity" Value="0.55" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="StatusBarPickerComboBoxStyle"
                   TargetType="ComboBox"
                   BasedOn="{StaticResource {x:Type ComboBox}}">
                <Setter Property="Tag" Value="NoOutlineBubble" />
                <Setter Property="Background" Value="{DynamicResource TranscriptBubbleGlassStrongBrush}" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="ItemContainerStyle" Value="{StaticResource NoOutlineBubbleComboBoxItemStyle}" />
            </Style>
        </UserControl.Resources>
        <Border
                Background="{DynamicResource SurfaceContrastBrush}"
                BorderBrush="{DynamicResource BorderStrongBrush}"
                BorderThickness="1,1,0,0"
                CornerRadius="10,10,0,0"
                Padding="12,7"
                Margin="{DynamicResource StatusBarMargin}">
            <DockPanel>
                <WrapPanel DockPanel.Dock="Right" VerticalAlignment="Center">
                    <ComboBox x:Name="CmbApprovalPolicy"
                              Style="{StaticResource StatusBarPickerComboBoxStyle}"
                              Width="150"
                              Margin="0,0,8,0"
                              IsEnabled="{Binding TurnComposer.IsApprovalPolicyEnabled}"
                              SelectedValuePath="Content"
                              SelectedValue="{Binding TurnComposer.SelectedApprovalPolicy, Mode=TwoWay}"
                              ToolTipService.InitialShowDelay="60"
                              ToolTipService.BetweenShowDelay="50"
                              ToolTipService.ShowDuration="45000">
                        <ComboBox.ToolTip>
                            <ToolTip Style="{StaticResource StatusBarHoverToolTipStyle}"
                                     Content="Approval policy" />
                        </ComboBox.ToolTip>
                        <ComboBoxItem Content="on-request" />
                        <ComboBoxItem Content="on-failure" />
                        <ComboBoxItem Content="untrusted" />
                        <ComboBoxItem Content="never" />
                    </ComboBox>

                    <ComboBox x:Name="CmbSandbox"
                              Style="{StaticResource StatusBarPickerComboBoxStyle}"
                              Width="175"
                              IsEnabled="{Binding TurnComposer.IsSandboxEnabled}"
                              SelectedValuePath="Content"
                              SelectedValue="{Binding TurnComposer.SelectedSandbox, Mode=TwoWay}"
                              ToolTipService.InitialShowDelay="60"
                              ToolTipService.BetweenShowDelay="50"
                              ToolTipService.ShowDuration="45000">
                        <ComboBox.ToolTip>
                            <ToolTip Style="{StaticResource StatusBarHoverToolTipStyle}"
                                     Content="Sandbox mode" />
                        </ComboBox.ToolTip>
                        <ComboBoxItem Content="workspace-write" />
                        <ComboBoxItem Content="read-only" />
                        <ComboBoxItem Content="danger-full-access" />
                    </ComboBox>
                </WrapPanel>

                <TextBlock Text="Status" Style="{StaticResource CaptionTextStyle}" Margin="0,0,8,0" VerticalAlignment="Center" />
                <TextBlock x:Name="LblStatus" Text="{Binding StatusText}" VerticalAlignment="Center" />
            </DockPanel>
        </Border>
</UserControl>
